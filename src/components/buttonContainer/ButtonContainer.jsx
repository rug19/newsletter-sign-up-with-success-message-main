import './buttonContainer.css';

export default function ButtonContainer() {
  return (
    <section>
      <button className='form_button' id="submit-btn">Subscribe to monthly newsletter</button>
    </section>
  );
}
